<?php require_once __DIR__.'/../includes/functions.php'; $t=$_GET['token']??''; if($t){$stmt=db()->prepare('UPDATE users SET email_verified_at=?, email_verify_token=NULL, updated_at=? WHERE email_verify_token=?');$stmt->execute([now(),now(),$t]); flash($stmt->rowCount()?'success':'error',$stmt->rowCount()?'Email verified.':'Invalid verification token.');} redirect('auth/login.php');
